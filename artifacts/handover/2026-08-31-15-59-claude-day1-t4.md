# Day1 T2·T3·T4 검증 + T4 구현 인수인계

- 2026-08-31 15:59
- 도구: claude
- 선행: `handover/2026-08-30-16-01-claude-schema-t3.md`

## 이번 세션에 한 일

### T1·T2·T3 재검증 (직접 실행)
- `tsc --noEmit` / `eslint .` / `next build` 통과. `next dev` GET / 200.
- T3 스모크(`server/jobs/verify-schema.ts`) 전체 통과 → 마이그레이션 이미 적용됨, RLS·트리거·dedup 정상.
- 이슈: 이 Windows 환경 첫 컴파일 느림(dev ~40s, build 1분+), 직전 세션 좀비 `next dev` 6개가 락 점유 → 정리함. 자세히: `artifacts/ai-notes/T1 검증 중 빌드·개발서버 멈춤.md`.
- Codex 위임 시도 → Windows 샌드박스가 홈 디렉터리 차단(EPERM)해서 직접 실행으로 전환.

### T4 DART API 연동 (신규 구현)
- **설계 변경(사용자)**: corp_code 목록을 인메모리 캐시 대신 **Supabase `dart_corp_codes` 테이블**에 영속화. 2일차 GH Actions cron이 동기화 담당.
- 마이그레이션 `supabase/migrations/20260831120000_dart_corp_codes.sql` — 사용자가 대시보드에서 실행 완료. RLS on, 정책 없음 → service-role 전용.
- `server/dart/` : `types.ts` `config.ts` `http.ts`(dartGet + status 매핑) `corp-code-file.ts`(ZIP→fflate→fast-xml-parser) `corp-codes.ts`(resolveCorp) `client.ts`(fetchCompanyProfile / fetchKeyFinancials, 연결 우선, year 폴백 1회).
- `server/jobs/sync-dart-corp-codes.ts` — 전체 목록 배치 upsert. 실행 완료 → **118,810행 적재**.
- `app/api/company/dart/route.ts` — `GET ?corp=<회사명>` 또는 `?corp_code=`. 로그인 확인(401) → resolveCorp(404) → 개황+재무 병렬. `runtime="nodejs"`.
- 새 의존성: `fflate`, `fast-xml-parser`.
- 스모크(`server/jobs/verify-dart.ts`) 통과: 삼성전자 → 00126380 / 005930, 2025 연결재무 정상.

### 버그 수정
- `[fix] fe6d1e4`: fast-xml-parser가 `stock_code`(005930)·`corp_code`를 숫자로 파싱해 앞자리 0 유실 → `parseTagValue:false`. 재동기화함. 노트: `artifacts/ai-notes/fast-xml-parser 숫자 파싱으로 앞자리 0 유실.md`.

## git 상태

- `feat/data-layer` 브랜치에서 태스크 스텝별 커밋(`[feat] T2` / `[feat] T3` / `[chore]` / `[feat] T4 ×3` / `[fix]`) → `main`에 `--no-ff` 병합 (`0eb3970`).
- **main이 origin/main보다 8커밋 앞섬. 아직 push 안 함** (사용자 지시 대기).
- `feat/data-layer` 로컬 브랜치 남아 있음 (삭제 안 함).
- 커밋 규칙: `feat/<kebab>` 브랜치, `[tag] 한글` 커밋, 태스크 스텝별, 검증 후 커밋. `artifacts/`·`ai-notes/`·`handover/`는 gitignore.

## 다음 세션 (T5)

- **T5. 수동 공고 입력 UI + 저장** — `src/entities/job-posting/{model,api}.ts`, `src/features/add-job-posting/`, `src/views/dashboard` 통합. `day1.md` T5 참고.
- 새 브랜치 필요 (예: `feat/job-posting-input`). 착수 전 계획 모드 권장.
- 참고: `job_postings` 테이블은 T3에서 생성됨. dedup 제약 `(user_id, company_key, role_norm, employment_type, posted_at)` nulls not distinct. `company_id`는 nullable — 회사 해석은 Route Handler 몫(추후).

## 미해결 / 주의

- Route Handler `/api/company/dart` curl 검증은 **인증 구현(8~9일차) 후** 가능 (로그인 세션 필요). 지금은 verify-dart.ts로 모듈 검증.
- 로컬 Node 20.14 ↔ Supabase: Supabase 클라이언트 쓰는 tsx 스크립트·`pnpm dev`는 `NODE_OPTIONS=--experimental-websocket` 필요. 근본 해결은 Node 22.
- `pnpm lint`는 빈 슬라이스(`src/entities|features|widgets`) 때문에 `fsd/insignificant-slice` 3건 exit 1 — T5/T6에서 채워지면 해소.
- supabase-js의 `select('*')` 반환 추론이 수기 `Database` 타입과 안 맞아 `corp-codes.ts`에서 `as CorpRow[]` 캐스트함. T3 이후 계속 있는 마찰.
