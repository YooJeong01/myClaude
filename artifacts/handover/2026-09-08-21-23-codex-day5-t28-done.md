- 2026-09-08 21:23
- 작업: T28 매직링크 로그인 최소 구현 완료
- 구현:
  - `src/features/auth/` server action, 로그아웃 action, 로그인 폼, 공개 export 추가
  - `app/login/page.tsx`, `app/auth/confirm/route.ts` 추가
  - hero 대시보드 진입점 로그인 상태 분기
  - dashboard 헤더 로그아웃 버튼 추가
- 검증:
  - `cmd /c pnpm exec tsc --noEmit` 통과
  - `cmd /c pnpm lint` 통과
  - `cmd /c pnpm build` 통과
- 주의:
  - Supabase Redirect URLs에 로컬 `http://localhost:3000` 및 배포 도메인 등록 필요
  - 기존 미추적 파일 `.claude/settings.local.json`은 건드리지 않음
