# 인수인계 — Day 4~7 완료 / 배포 / 리디자인·앱확장 방향 확정 / Day 8 대기

- 2026-09-10 00:34
- 도구: claude
- 선행: `artifacts/handover/2026-09-09-07-00-claude-day7-delegation.md` (그 이후 매우 긴 세션)

## 이 세션에 한 일 (요약)

### 구현 (전부 `main` 병합·`origin/main` push 완료)
- **Day 4 (T24~T27)** 지원동기 매칭 백엔드 — 마이그레이션 실행 후 verify 통과, 병합.
- **Day 5~6 (T28~T34)** 온디맨드 리포트 UI — 매직링크 로그인 / 경험 CRUD / 기업분석·지원동기 화면 /
  이력·신선도 / IndexedDB 오프라인 미러 / Playwright E2E. E2E 9/9.
- **Day 6 후속** — 공고 검색·커서 페이지네이션 / 기업분석 모아보기(`/dashboard/analyses`) /
  관심공고 북마크(`saved_postings`) / 캘린더(`/dashboard/calendar`, react-big-calendar) /
  스크래핑 3단계 직무 필터(relevant/review/irrelevant) / 기업규모 추정(DART 휴리스틱).
- **Day 7 (T42~T48)** — 원티드·점핏·지행 스크래퍼(JSON API) / 크로스-사이트 dedup 설계 /
  role-filter techStacks 반영. GHA `scrape-postings.yml` 에 `scrape-api-sites` job 추가.
- **버그 수정**:
  - 사람인 스크래퍼 `searchKeyword`→`searchword` (키워드 검색이 안 먹어 전체목록을 긁고 있었음).
  - 사람인 파서 셀렉터 전면 교정 — 회사명이 957건 전부 "기업정보"(UI 라벨)였음. `.corp_name` /
    `.job_day`(게시일) / `.date`(마감일) / `.job_condition`(고용형태) 로 수정.
  - dedup `role_norm` 과다 정규화 → 최소 정규화(소문자+공백만). `company_key` 강화는 유지.
  - 스크래핑 강도 완화: `SCRAPE_MAX_PAGES` 기본 10→3, 요청 지연에 0~1500ms 지터.
  - verify-schema `motivation_drafts` 불변성 단언 교정(RLS 0행 필터링을 에러로 기대했음).

### 배포
- **프로덕션 라이브: `https://my-claude-ruby.vercel.app`** (사용자가 Vercel env 등록 + redeploy).
- 라우트·Supabase 연결 확인 (미들웨어 리다이렉트, API 401 정상). Supabase Redirect URLs 에
  로컬 + 프리뷰 + 프로덕션 도메인 등록됨.

### 방향 확정 (상세: `ai-notes/2026-09-10 앱 확장·리디자인 방향 결정.md`)
- **앱 확장 = Capacitor(모바일) + Tauri(데스크톱)**, 웹 코드베이스 1개, 배포 URL 로드. → `day8.md` (T49~T55).
  기각: PWA(사용자 제외), RN 셸(데스크톱 불가), Electron(무거움·데스크톱만).
- **전체 리디자인** — 디자인 시스템 파운데이션 → 컴포넌트 → 화면. 시각 방향은 **사용자가 2026-09-10 지시 예정.**
- 작업 순서: **리디자인 → Day 8(Capacitor+Tauri) → Day 9(배포·인증 하드닝) → Day 10(QA) → Day 11~14(포폴).**

## 현재 상태 (태스크 기준)

- Day 1~7 ✅ 전부 `main` 병합·push.
- 배포 ✅ (기본). 남은 하드닝은 Day 9.
- ⬜ **리디자인** — 시각 방향 대기 중 (다음 첫 작업).
- ⬜ Day 8 (Capacitor + Tauri) — `day8.md` 작성됨, 리디자인 완료가 선행.
- ⬜ Day 9 (프로덕션 SMTP / Google OAuth 여부 / RLS 재점검).
- ⬜ Day 10 QA / Day 11~14 포트폴리오.

## git 상태

- `main` == `origin/main` == `2f57974`. **동기화됨. 이번 세션 전부 push 완료.**
- 스태일 로컬 브랜치 `feat/data-layer` (예전부터, 무관, 삭제 안 함).
- `next-env.d.ts` 는 로컬 빌드로 계속 바뀜 — **커밋 금지** (`git checkout -- next-env.d.ts`).

## 🔜 다음 첫 작업 — 리디자인

1. 사용자에게 **시각적 방향**을 받는다: 무드(미니멀/토스풍/리니어풍/개발자도구풍/에디토리얼),
   레퍼런스, 다크모드 여부. `design` 스킬 목업 먼저 vs 코드부터.
2. 방향 확정 후 리디자인 태스크 문서(day7.5 or `design.md`) 작성 → 디자인 시스템 파운데이션부터.
3. 리디자인 시 미리 반영: safe-area inset 토큰, 터치 타깃, hover-only 제거, 360px~데스크톱 대응
   (day8.md T50 연결고리).

## 미결 / 주의

- **미실행 마이그레이션**: `supabase/migrations/20260909075000_dedup_normalization.sql`.
  크로스-사이트 중복이 실제로 안 생겨서(최소 정규화) 보류 중. 실행해도 삭제될 것 0 → 무해하나 급하지 않음.
- **잡코리아 스크래퍼**: RSC SPA 로 바뀌어 0건. 보류. (원티드·점핏·지행이 대체)
- **Gmail 이메일 수집 워크플로 실패 중**: refresh_token 7일 만료(Testing 모드). 프로덕션 유지하려면
  OAuth 앱 게시 필요 — Day 9.
- **캐치 이메일 파서 스텁** (`server/gmail/parse.ts:124`).
- **Gemini 무료 티어 과부하**: `/api/motivation` 등이 간헐 502. 재시도로 버티는 중. Day 9~10 판단.
- **호스트 메모리 부족**: 이 세션 내내 `codex exec` 와 `pnpm build` 가 가용 RAM ~1GB 에서 반복 강제 종료.
  Codex 위임 시 죽으면 커밋된 데까지 Claude 가 인계하는 패턴으로 진행함. build 는 메모리 여유 시 확인.
- **테스트 데이터**: `job_postings` ~1156건 (사람인 958 + 캐치 41 + 원티드 36 + 점핏 27 + 지행 21 + 잡코 1).
  `motivation_drafts`·`company_analyses` 에 테스트로 생긴 행 다수 — 데모 데이터로 둠.

## 참고 문서

- `artifacts/tasks/day5.md` ~ `day8.md` — 태스크 스펙.
- `artifacts/ai-notes/2026-09-08 day4...` / `2026-09-09 day6 야간 진행.md` / `2026-09-09 day7 진행.md`
  / `2026-09-10 앱 확장·리디자인 방향 결정.md` — 진행·결정 로그 (로컬 전용, gitignore).
- `AGENTS.md` — 협업 규율. 핀 메모리: Claude 계획 / Codex 구현 (+ 검증·튜닝·감시·되돌리기 어려운 DB 조작·
  Codex 사용불가 시엔 Claude 직접 허용).
