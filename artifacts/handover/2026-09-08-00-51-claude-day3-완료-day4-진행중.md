# 인수인계 — Day 3 완료 / Day 4 진행 중 (마이그레이션 대기)

- 2026-09-08 00:51
- 도구: claude
- 선행: `artifacts/handover/2026-09-07-16-52-claude-git-workflow-fix.md` (그 이후 세션)

## 이 세션에 한 일 (요약)

1. **타입 부채 정리** (`fix/type-debt` → main) — Day 2 병합 tsc 에러 16건 해소.
   핵심: `server/supabase/types.ts` 빈 네임스페이스를 `Record<string, never>` → `{ [_ in never]: never }`
   (supabase-js 2.75 의 `from()` 이 View 오버로드로 새서 select 가 `{}` 되던 근본 원인). T3 부터의 마찰 해소.
2. **T19 한경컨센서스 수집** (`feat/hankyung-consensus` → main) — Codex 위임. `server/hankyung/`.
   consensus.hankyung.com/analysis/list 정적 HTML 파싱, 원문 PDF 재게시 없이 메타데이터만.
3. **태스크 문서 정비** — `artifacts/tasks/day2.md`(소급, T8~T18 전부 완료 표시), `day3.md`, `day4.md` 신규.
   `day1.md` stale 체크박스 갱신.
4. **LLM 제공자 결정 → Gemini 무료 티어** — 자세히 아래.
5. **Day 3 (T20~T23) 완료** (`feat/llm-client`, `feat/company-analysis` → main) — Claude 직접 구현.
   기업분석 종합 파이프라인 완성.
6. **Day 4 (T24~T27) 구현** (`feat/motivation-matching`, **main 미병합**) — Codex 위임. 코드 리뷰 통과.
   **마이그레이션 실행 대기 중.**

## ⚠️ 새 작업 방식 (이번 세션에 사용자가 확정 — 메모리에도 있음)

- **계획·설계·검증은 Claude, 구현은 Codex 위임.** 위임 가능한 코드 작업을 Claude 가 직접 하지 않는다.
- Claude 는: 태스크 문서(`artifacts/tasks/dayN.md`), 설계 결정 정리, Codex 결과물 검증·리뷰, Codex 가 못 하는 일.
- Codex 위임 방법: `codex exec --approve-for-me -C "C:/myClaude" "<지시>"` 백그라운드 실행
  (`< /dev/null` 붙일 것). `.claude/settings.json` 에 `Bash(codex exec:*)` 권한 있음.
  위임 지시서를 `artifacts/handover/` 에 먼저 남기고, Codex 는 `feat/<kebab>` 브랜치에서 작업하되
  `main` 병합·push 금지. Codex 는 PowerShell `Get-Content` 로 UTF-8 파일 읽을 때 `-Encoding UTF8` 안 주면
  한글 깨짐 — 지시서에 "UTF-8로 읽어라" 명시하면 됨.
- Day 3(T20~T23)는 이 규칙 확정 전이라 Claude 가 직접 했다. Day 4 부터 적용.

## LLM 제공자 (확정)

- **Google Gemini 무료 티어.** SDK `@google/genai` (2.21.0), 모델 **`gemini-3.6-flash`**, env `GEMINI_API_KEY`.
  - `gemini-2.5-flash` 는 신규 사용자에게 404 ("no longer available to new users") → API 안내대로 3.6-flash.
  - 무료 한도 ≈ 10 RPM / 250 RPD. "다시 분석하기" 수동 버튼이라 충분.
- Anthropic/OpenAI API 는 구독과 별도 유료라 기각. GitHub Models 는 2026-07-30 완전 종료 확인.
- 사용자는 `ujjh77@gmail.com` 구글 계정으로 aistudio.google.com 에서 키 발급, `.env.local` 에 넣어둠.
- `server/llm/` 이 유일한 LLM 진입점. `generateText()` / `generateJson<T>(prompt, schema)`.
  새 제공자·새 SDK 추가 금지.

## 현재 git 상태

- `main` 이 `origin/main` 보다 **13커밋 앞섬. push 안 함** (사용자 지시 시에만 push).
  이번 세션 커밋 전부 `feat/*` / `docs/*` 브랜치에서 `--no-ff` 병합. main 직접 커밋 없음.
- **`feat/motivation-matching` 브랜치 — main 미병합, 5커밋.** Day 4 코드. 마이그레이션 실행 후 검증→병합 예정.
  - `9420873` 스키마 / `6ee26a7` 생성 로직 / `c641453` API / `7ea441c` 검증 스모크 / `faa7fc5` 완료 노트
- `feat/data-layer` — 예전부터 남아있는 로컬 브랜치 (무관, 삭제 안 함).
- `next-env.d.ts` 는 로컬 `pnpm dev`/`build` 로 자꾸 바뀜 — Next 자동생성, 커밋하지 말 것.

## 🔜 내일 첫 작업 — Day 4 마무리

### 1. (사용자) 마이그레이션 실행
`supabase/migrations/20260907233000_day4_motivation.sql` 를 Supabase 대시보드 SQL Editor 에서 Run.
`user_experiences` + `motivation_drafts` 두 테이블 생성 (RLS·트리거·인덱스 포함).

### 2. (Claude) 검증
`feat/motivation-matching` 체크아웃 후:
```
pnpm exec tsx --env-file=.env.local server/jobs/verify-schema.ts
pnpm exec tsx --env-file=.env.local server/jobs/verify-motivation.ts
```
- verify-schema: 두 신규 테이블 케이스 통과 확인
- verify-motivation: `SCRAPE_OWNER_USER_ID` + admin 으로 기존 `company_analyses` 행 1개 + 임시 경험 2~3개
  → `generateMotivation` → `motivation_drafts` insert. `MotivationResult.angles` 채워짐 확인.
  (기존 `company_analyses` 행 있음 — 이 세션에 삼성전자×2, 카카오×1 생성됨)
- `tsc --noEmit` / `pnpm lint` / `pnpm build` 재확인

### 3. (Claude) main 병합
검증 통과하면 `feat/motivation-matching` 을 `--no-ff` 로 main 병합, 브랜치 삭제.
day4.md T24~T27 체크박스 `[완료]` 로 갱신 (별도 docs 커밋).

### 4. 이후 — Day 5~6 (온디맨드 리포트 UI)
goal.md 5~6일차. Claude 가 `artifacts/tasks/day5.md` 작성 → Codex 위임.
포함: 경험 입력 폼 + 대시보드 통합(Day 4 에서 미룸), 분석/지원동기 결과 화면,
오프라인 지원(IndexedDB 미러, "N일 전" 신선도, "다시 분석하기" 버튼, "지난 분석 목록").

## Day 4 코드 리뷰 메모 (Codex 결과물)

- `server/motivation/{types,prompt,generate}.ts` — `server/analysis/` 패턴 그대로. D1 스키마
  (`angles[]`: point / matched_experience / connection / draft_sentences, + `summary_paragraph`) 정확.
- `app/api/motivation/route.ts` — **SSR 클라이언트**(쿠키, RLS)로 본인 분석·경험만 조회.
  `POST { company_analysis_id, experience_ids[], role? }`. 404/400/LlmError 매핑. `runtime = "nodejs"`.
- 스펙 이탈 1건(문제 없음): `company_analyses` 행에 회사명이 없어서 `companies.name` 별도 조회해 `companyName` 으로 전달.
- `server/supabase/types.ts` — 두 테이블 수동 동기화 완료, 빈 네임스페이스 shape 안 건드림.
- Codex done 노트: `artifacts/handover/2026-09-07-23-39-codex-day4-done.md`

## 진행 상태 (태스크 기준)

- Day 1 (T1~T7) ✅ / Day 2 (T8~T18) ✅ / 타입부채 ✅
- Day 3 (T19~T23) ✅ — 기업분석 종합: `POST /api/company/analyze`, `company_analyses` 저장까지 E2E 검증
- Day 4 (T24~T27) 🔧 진행 중 — 코드 완료(`feat/motivation-matching`), 마이그레이션+검증+병합 남음
- Day 5~6 (UI) ⬜ / Day 7 (워크넷) ⬜ / Day 8~9 (실인증·배포) ⬜ / Day 10~ (QA·포폴) ⬜

## 미결 / 주의

- 테스트 데이터: `company_analyses` 에 이 세션 verify 로 생긴 행 3개(삼성전자×2, 카카오×1, `SCRAPE_OWNER_USER_ID` 소유).
  데모 데이터로 둬도 되고 정리해도 됨. Day 4 verify 도 `motivation_drafts` 에 행 남길 예정.
- 실제 인증 아직 없음(Day 8~9). 모든 API 는 verify-*.ts 스크립트(admin + `SCRAPE_OWNER_USER_ID`)로만 검증 가능.
- Vercel 프로덕션 env 에 `GEMINI_API_KEY` 등록 필요 (Day 8~9 배포 시).
- 무료 티어: 회사 1개 완주에 LLM 2회(분석+매칭). 250 RPD 여유.
