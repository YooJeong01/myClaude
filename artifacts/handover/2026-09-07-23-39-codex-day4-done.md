# Day 4 지원동기 매칭 완료 (Codex)

- 완료 시각: 2026-09-07 23:39
- 브랜치: `feat/motivation-matching`
- main 병합: 안 함
- push: 안 함

## 생성/수정 파일

- `supabase/migrations/20260907233000_day4_motivation.sql`
- `server/supabase/types.ts`
- `server/jobs/verify-schema.ts`
- `server/motivation/types.ts`
- `server/motivation/prompt.ts`
- `server/motivation/generate.ts`
- `app/api/motivation/route.ts`
- `server/jobs/verify-motivation.ts`

## 마이그레이션

- 파일: `supabase/migrations/20260907233000_day4_motivation.sql`
- 사용자가 Supabase SQL Editor 에서 직접 실행해야 함
- 요약:
  - `user_experiences` 생성: 사용자 소유 경험, `updated_at` moddatetime 트리거, `(user_id, created_at desc)` 인덱스
  - `motivation_drafts` 생성: `company_analyses` 기반 불변 이력, `experience_ids uuid[]` 스냅샷, `(user_id, company_analysis_id, created_at desc)` 인덱스
  - RLS:
    - `user_experiences`: authenticated 본인 행 전체
    - `motivation_drafts`: authenticated 본인 select/insert, update/delete 정책 없음

## 검증 결과

- `pnpm exec tsc --noEmit`: 통과
- `pnpm lint`: 통과 (`eslint . && steiger ./src`)
- `pnpm build`: 통과
- `pnpm exec tsx --env-file=.env.local server/jobs/verify-schema.ts`: 실패
  - 기존 테이블 확인은 통과
  - `public.user_experiences` 없음: 새 마이그레이션 미실행 상태로 판단
- `pnpm exec tsx --env-file=.env.local server/jobs/verify-motivation.ts`: 실패
  - 기존 `company_analyses` 조회 성공
  - `public.user_experiences` insert 에서 테이블 없음: 새 마이그레이션 미실행 상태로 판단

## 커밋 해시

- `9420873` `[feat] 지원동기 매칭 스키마 추가`
- `6ee26a7` `[feat] 지원동기 매칭 생성 로직 추가`
- `c641453` `[feat] 지원동기 매칭 API 추가`
- `7ea441c` `[chore] 지원동기 매칭 검증 스모크 추가`

## 스펙 이탈

- 없음
- `company_analyses` 행에는 회사명이 직접 없으므로, `/api/motivation`에서 SSR 클라이언트로 `companies.name`을 별도 조회해 `MotivationInput.companyName`에 전달함

## 미해결/주의 사항

- Supabase 대시보드에서 `20260907233000_day4_motivation.sql` 실행 후 `verify-schema.ts`, `verify-motivation.ts` 재실행 필요
- 작업 전부터 untracked 상태였던 `.claude/settings.local.json`, `artifacts/handover/2026-09-07-23-28-claude-day4-motivation-delegation.md`는 커밋하지 않음
