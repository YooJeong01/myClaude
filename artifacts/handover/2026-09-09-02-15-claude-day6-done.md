# Day 6 완료 — 탐색·수집·북마크 개선 (T35~T41)

- 2026-09-09 02:15
- 도구: claude (Codex 위임 구현 T38~T37 + Claude 검증/병합/T41 마무리)
- 선행: `artifacts/handover/2026-09-09-00-05-claude-day6-delegation.md`,
  `artifacts/handover/2026-09-09-codex-day6-progress.md` (Codex 진행 로그)

## 결과

T35~T41 전부 `main` 병합. `feat/day6` 삭제. `main` 은 `origin/main` 보다 앞서며 **push 안 함**.
- `4bdb95e [merge] feat/day6` — T38·T39·T40·T36·T35·T37 (Codex 6개)
- `f2fac53 [merge] feat/day6` — T41 (Codex OOM 사망으로 미커밋이던 WIP 을 Claude 가 완료)

## 태스크 (전부 완료·병합)

| 태스크 | 산출물 |
|---|---|
| T38 URL 단건 파싱 | `server/job-postings/parse-url.ts` (JSON-LD/OG→LLM 폴백), `app/api/job-posting/parse/route.ts`, add-job-posting 폼 "URL로 채우기", `verify-parse-url.ts` |
| T39 관심 공고 북마크 | `20260909005000_saved_postings.sql`(사용자 실행 완료), `server/supabase/types.ts` 동기화, `src/entities/saved-posting/`, `src/features/toggle-saved-posting/`, 대시보드·분석 통합 |
| T40 캘린더 | `react-big-calendar`+`date-fns`, `app/(app)/dashboard/calendar/page.tsx`, `src/widgets/saved-calendar/` (월뷰, 게시일~마감일 막대, 상시채용=하루, 클릭 시 raw_text 발췌+링크) |
| T36 기업분석 모아보기 | `listAnalysesGrouped`, `app/(app)/dashboard/analyses/page.tsx` (회사별 카드·검색·커서), 진입점 링크 |
| T35 공고 검색·페이지네이션 | `listJobPostings(supabase, opts?)` 확장(q/필터/range/count/커서, 하위호환), 대시보드 필터 UI |
| T37 스크래핑 직무 필터 | `server/scraping/common/role-filter.ts`, 각 스크래퍼 적용, `audit-postings.ts`, `verify-role-filter.ts`, `.env.example` 키워드 갱신 |
| T41 기업 규모 | `server/analysis/company-size.ts` `estimateCompanySize` (DART corpClass+재무+업력 휴리스틱), `ANALYSIS_SCHEMA_VERSION` 1→2, synthesize 코드 stamp, 배지, `verify-company-size.ts` |

## 협업 중 문제

1. **Codex 프로세스 OOM 강제 종료** (Day 5 T34 때와 동일) — T38~T37 커밋 후 T41 작업 중.
   T41 코드는 작성됐으나 미커밋. Claude 가 stash 복구 → 검증 → 커밋 → 병합.
   근본 원인: 로컬 가용 메모리 ~2.6GB (전체 8GB). Codex(gpt-5.5 에이전트) + node 빌드가 한계 초과.
2. **day6 E2E 시나리오 미추가** — Codex 가 day6.md T34 확장(검색/북마크/캘린더/모아보기 시나리오)에
   도달하기 전 종료. 현재 day6 신규 기능은 `verify-*.ts` 스모크만, 브라우저 E2E 커버리지 없음.
   회귀 검증은 기존 `e2e/day5-report.spec.ts` 6/6 통과로 확인(대시보드 대폭 변경에도 회귀 없음).

## 검증

- `main` 병합 후 `pnpm exec tsc --noEmit` / `pnpm lint`(eslint + steiger, FSD 위반 0) / `pnpm build` 그린.
  새 라우트: `/api/job-posting/parse`, `/dashboard/analyses`, `/dashboard/calendar`.
- E2E `e2e/day5-report.spec.ts` 6/6 통과 (프로덕션 서버, 1.2분). 회귀 없음.
- `verify-company-size.ts` 4케이스, `verify-role-filter.ts`(Codex 확인), `verify-schema.ts`(saved_postings 케이스).

## 가정한 결정 (day6.md, 아침 사용자 확인)

- T37 직무 화이트리스트 = 프론트엔드/웹개발/퍼블리셔/리액트/vue/풀스택. 백엔드·앱·데이터·PM 제외.
- T37 기존 데이터: **삭제 안 함.** `audit-postings.ts` dry-run 결과 **331건 중 293건(89%)이 범위 밖.**
  삭제 결정 시 실삭제 모드 추가 필요.
- T38: 사용자가 준 단일 URL 1회 fetch (robots 미확인, 크롤링 아님).
- T40: `react-big-calendar` 채택. 이벤트 요약 LLM 안 씀.
- T41: DART 기반 휴리스틱만(sminfo API 미사용). 기존 v1 분석 행은 규모 없음(옵셔널).

## 미결 / 다음

- **아침 사용자 판단 5건** — day6.md "아침 확인·판단" 또는 `ai-notes/2026-09-09 day6 야간 진행.md` 참고.
- day6 기능 E2E 시나리오 추가 (다음 세션).
- JobKorea 수집분 role 필드에 네비게이션 텍스트 저장됨 (T12 파서 결함 추정, 별개 이슈 — 백로그).
- 다음 태스크: Day 7(워크넷 — 조건부) 또는 Day 8~9(실인증·배포). Vercel 주소 등록됨:
  `https://my-claude-n4b5u7hzw-uzzangs-projects.vercel.app/`
- `next-env.d.ts` 커밋 금지.
